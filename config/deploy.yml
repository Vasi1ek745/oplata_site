service: oplata_site
image: oplata_site

servers:
  web:
    - 194.87.98.136

proxy:
  app_port: 3000
  healthcheck:
    path: /up
    port: 3000
    interval: 5
    timeout: 10

ssh:
  user: vasiliy

registry:
  server: localhost:5000
  username: dummy
  password: dummy

env:
  clear:
    RAILS_ENV: production
    PORT: 3000
    RAILS_SERVE_STATIC_FILES: "true"

  secret:
    - RAILS_MASTER_KEY
    - SECRET_KEY_BASE

builder:
  arch: amd64
  dockerfile: Dockerfile
