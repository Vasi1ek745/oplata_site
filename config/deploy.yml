service: oplata_site
image: oplata_site

servers:
  web:
    - 194.87.98.136
    volumes:
      - /home/vasiliy/Изображения:/rails/public/images

proxy:
  app_port: 3000

ssh:
  user: vasiliy

registry:
  server: localhost:5000
  username: dummy
  password: dummy

env:
  clear:
    RAILS_ENV: production
    PORT: 3000
    RAILS_SERVE_STATIC_FILES: "true"

  secret:
    - RAILS_MASTER_KEY
    - SECRET_KEY_BASE

builder:
  arch: amd64
  dockerfile: Dockerfile
