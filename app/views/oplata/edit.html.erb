<% content_for :title, "Редактирование платежа" %>

<div class="md:w-2/3 w-full mt-8">
  <h1 class="font-bold text-3xl mb-6">Редактирование платежа</h1>

  <%= render "form", oplatum: @oplatum %>
</div>